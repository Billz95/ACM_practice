```c++
// Kruskal’s Algorithm

int parent[N];
int size[N];
pair<int, pair<int, int>> edges[E]; // each edge is (weight, (u, v))
vector<int> mst;

int root(int x) {
     find so we know they’re connected
        join(edges[i].second.first, edges[i].second.second);
    }
    return 0;
}
```
